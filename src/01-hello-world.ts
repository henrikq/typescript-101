export {};

const message = "Hello World!";
console.log(message);

// `message` is implicitly "Hello World!" of type `string`
// typescript/vscode adds autocomplete to foo
// , including all methods from String.prototype
